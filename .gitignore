# Credentials — NEVER commit these
.env
*.env
*credentials*.json
client_secret*.json
service_account*.json
config/applicant.json
gcp/
.gcp/

# Runtime data — regenerated each run
data/resume.pdf
data/jobs_*.json
data/new_jobs.json
data/run_summary.json
data/errors.log
data/failed_sheet_writes.json
data/dry_run_log.jsonl
data/unanswered_questions.log
data/apply_logs/
data/chrome_profile/

# Keep the dedup cache so it acts as a local backup
# (data/applied_jobs.json is intentionally tracked)

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Playwright
.playwright/

# Claude Code local runtime state
.claude/

# Personal artifacts that live alongside the repo but aren't tracked
Daniel_Lonkry_Resume.pdf
*.xlsx
